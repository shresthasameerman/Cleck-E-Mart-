/**
 * Cart AJAX Handler
 * Handles dynamic cart updates via AJAX
 */

document.addEventListener('DOMContentLoaded', function () {
    // Handle cart form submissions via AJAX
    // Regular form submission is used instead of AJAX
    // This ensures cart updates work reliably with the existing cart.php logic
});
